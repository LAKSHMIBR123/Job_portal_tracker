# Netlify: set site "Base directory" to job-portal-frontend if your repo root is above this folder.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
